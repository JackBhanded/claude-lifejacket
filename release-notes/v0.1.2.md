# Claude Lifejacket v0.1.2 — a new look + a tray companion

A gorgeous redesign, plus Lifejacket now lives in your system tray so it can keep
every Claude session aware of your projects without a window in the way.

## What's new

- **A gorgeous new look (elevated Claude-brew + dark mode).** The dashboard is now frosted glassmorphism over a soft drifting aurora — a count-up stat row, a glassy project grid, status-light surfaces, the verbatim digest, and a sleek dark mode. The double-click window is restyled to match (gradient buttons, soft cards, light/dark toggle).
- **System-tray companion.** Run `lifejacket tray` (or just close the window) and
  Lifejacket tucks into your tray with a small life-vest icon. Right-click for
  **Open / Sync now / Open dashboard / Quit**; double-click to reopen the window.
  Closing the window now hides to the tray instead of quitting, so auto-sync keeps
  working quietly in the background.
- **`lifejacket tray`** command to launch straight into the tray.
- **Run at startup.** A "Run at startup" toggle in the tray menu pins Lifejacket to
  your per-user Windows startup (no admin needed), so it keeps every session
  project-aware from the moment you log in.

## Refinements

- The tray icon is a distinct **life-vest glyph** in Claude orange, so you can
  tell Lifejacket apart from the other fleet tools at a glance in the tray. (The
  Claude logo stays the brand mark in the window, dashboard, and README.)
- The README now has an honest **"How it differs from the alternatives"** section.

## Install

Download **`Claude Lifejacket.exe`** below and double-click it — no Python, no
terminal. It opens a window with your projects; close it and it drops into your
tray.

Prefer the command line? `pip install --user .` then `lifejacket tray`.

---

Part of a little fleet: [Claude Meter](https://github.com/JackBhanded/claude-meter),
[Claude Lifeboat](https://github.com/JackBhanded/claude-lifeboat),
[Claude Compass](https://github.com/JackBhanded/claude-compass), and
[Claude Parachute](https://github.com/JackBhanded/claude-parachute).

**Full changelog:** https://github.com/JackBhanded/claude-lifejacket/compare/v0.1.1...v0.1.2
